Demo Co — 4-page static demo site

Preview locally:
- Open `index.html` in your browser, OR
- Run a simple server: `python -m http.server 8000` then visit http://localhost:8000

Notes:
- Theme: dark blue (`--surface: #0b3d91`, `--bg: #071733`)
- Files created: `index.html`, `about.html`, `services.html`, `contact.html`, `css/style.css`, `js/main.js`
- Contact form demonstrates client-side validation and mock submission only.

Tell me if you'd like alternate colors, images, or a deploy-ready setup (Netlify/GitHub Pages).